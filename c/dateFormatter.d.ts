export declare function formatDisplayDate(date: Date, format: string, formatter?: (date: Date) => string): string | undefined;
export declare function formatReturnDate(date: Date, format: string, formatter?: (date: Date) => any): any;
